@model IEnumerable<Accounting.DAO.Orders>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Number)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FK_Mat1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FK_Sup)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Status)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Number)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FK_Mat1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FK_Sup)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Status)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_ord }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_ord }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_ord })
        </td>
    </tr>
}

</table>
